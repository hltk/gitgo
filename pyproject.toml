[project]
name = "gitgo"
version = "0.1.0"
description = "GitGo HTML formatting setup"
requires-python = ">=3.8"
dependencies = []

[project.optional-dependencies]
dev = [
    "djlint>=1.35.0",
]

[tool.djlint]
profile = "golang"

